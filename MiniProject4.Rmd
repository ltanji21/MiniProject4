---
title: "MiniProject 4"
author: "Leigh Tanji, Samantha Bryce"
date: "2017年11月29日"
output: html_document
---

#Loading libraries
```{r}
suppressMessages(library(mdsr))
suppressMessages(library(RMySQL))
suppressMessages(library(dplyr))
suppressMessages(library(ggplot2))
```

```{r, message=FALSE, echo=FALSE}
cast_info <- dbConnect_scidb(dbname = "imdb")
class(cast_info)

cast_info <- cast_info %>%
  dbGetQuery("SELECT DISTINCT title.id, movie_id, title, kind_id, production_year, link_type_id
FROM imdb.title
JOIN movie_link
ON title.id = movie_link.movie_id
WHERE kind_id = '1';")
```





















